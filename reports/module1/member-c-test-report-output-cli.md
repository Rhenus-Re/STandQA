# 模块一 成员C 测试报告——输出与命令行域

被测版本：python-qrcode v8.2（commit `3704f57`）。
测试域：输出与命令行（`image/base.py`、`image/pil.py`、`image/pure.py`、
`image/svg.py`、`compat/*`、`console_scripts.py`、`main.py` 输出方法）。

## 1. 测试范围与目标

- 人工设计并实现 12 条用例，覆盖 PNG（PIL/PyPNG）、SVG、ASCII、TTY 与 CLI 入口。
- 覆盖等价类、边界值、场景法三种方法（硬性指标 ≥ 2 种）。
- 发现并验证 ≥ 1 个有效缺陷。
- 使用 `pytest` 一键运行。

## 2. 用例执行结果

| 阶段 | 总数 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| 修复前 | 12 | 11 | 1（TC-C-011） | 91.7% |
| 修复后 | 12 | 12 | 0 | 100% |

失败用例 TC-C-011 对应缺陷 C-BUG-01（`print_tty` 静区宽度忽略 `self.border`）。

## 3. 设计方法分布

| 方法 | 用例 | 数量 | 占比 |
|---|---|---|---|
| 等价类 | 001 / 004 / 006 / 007 / 010 | 5 | 41.7% |
| 边界值 | 002 / 009 | 2 | 16.7% |
| 场景法 | 003 / 005 / 008 / 011 / 012 | 5 | 41.7% |

## 4. 功能域覆盖

| 功能域 | 用例 | 覆盖点 |
|---|---|---|
| PNG/PilImage | 001 / 002 / 003 / 004 | 几何、最小参数像素、端到端保存、颜色三分支 |
| PNG/PyPNG | 005 / 006 | 端到端与跨后端一致、kind 校验、流/路径分支 |
| SVG | 007 / 008 | 根元素/命名空间/背景、path 合并 |
| ASCII/TTY | 009 / 010 / 011 | invert/border=0 边界、tty 守卫、静区宽度（缺陷回归） |
| CLI | 012 | --output PNG、--factory svg、--ascii |

## 5. 缺陷汇总

| 编号 | 严重程度 | 位置 | 状态 |
|---|---|---|---|
| C-BUG-01 | 一般（Major） | `qrcode/main.py` `print_tty` | 已修复并回归验证 |

详见 `member-c-defects-output-cli.md`。

## 6. 覆盖率（参考，全模块一汇总由成员A负责）

成员C用例对整个 `qrcode` 包的语句覆盖率为 68%（TOTAL）。关键被测文件：

| 文件 | 语句 | 未覆盖 | 覆盖率 |
|---|---|---|---|
| image/base.py | 75 | 5 | 93% |
| image/pil.py | 40 | 6 | 85% |
| image/pure.py | 31 | 1 | 97% |
| image/svg.py | 75 | 5 | 93% |
| console_scripts.py | 82 | 21 | 74% |
| main.py | 317 | 62 | 80% |

未覆盖部分主要为 `styledpil.py`、`styles/*`、`release.py`（模块一明确不测）
及 CLI 的 stdin/`--factory-drawer` 等非本域分支。

## 7. 结论

- 12 条用例全部通过，硬性交付指标（≥12 用例、≥2 种方法、≥1 缺陷、pytest 一键运行）均满足。
- 1 个有效缺陷已修复并经回归验证，修复补丁仅涉及 `print_tty` 静区宽度，风险可控。
- 交叉复现待成员B签字确认。
